spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/its_knowledge
    username: its_user
    password: its_pass

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.its: DEBUG
    dev.langchain4j: DEBUG